{
  "tool": {
    "type": "function",
    "function": {
      "name": "code_execution_tool",
      "description": "Run short and safe Python snippets for formula validation.",
      "parameters": {
        "type": "object",
        "properties": {
          "code": {
            "type": "string",
            "description": "Python code snippet under 900 characters."
          }
        },
        "required": [
          "code"
        ]
      }
    }
  },
  "runtime": {
    "entrypoint": "safe_code_execution.py",
    "callable": "run"
  }
}
